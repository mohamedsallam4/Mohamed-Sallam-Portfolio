import 'package:flutter/material.dart';

class AppColors {
  // الخلفية: أسود رمادي (Dark Grey)
  static const Color primaryBackground = Color(0xFF121212);
  // الخلفية الثانوية: رمادي غامق (Surface)
  static const Color secondaryBackground = Color(0xFF1E1E1E);
  
  // اللون الأساسي: ذهبي/أصفر (Amber)
  static const Color primaryColor = Color(0xFFFFC107);
  // لون التمييز: برتقالي محروق
  static const Color accentColor = Color(0xFFFFA000);

  // النصوص
  static const Color textPrimary = Color(0xFFEEEEEE);
  static const Color textSecondary = Color(0xFFB0B0B0);
  
  static const Color iconColor = Color(0xFFEEEEEE);
}
